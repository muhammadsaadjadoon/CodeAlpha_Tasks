# Models

Real training creates TorchScript checkpoints in `models/checkpoints/` and metrics in `models/reports/`.

No fake pretrained weights are included. Run the commands in `COMMANDS.md`, then register the MNIST digit specialist and EMNIST ByClass character model.
