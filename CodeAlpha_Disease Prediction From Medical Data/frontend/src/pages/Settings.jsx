import { useState } from 'react'
import { Icon } from '../components/Icons.jsx'

export default function Settings(){
 const [compact,setCompact]=useState(false); const [reduced,setReduced]=useState(false); const [tooltips,setTooltips]=useState(true)
 return <div className="page-stack"><div className="settings-intro"><span className="section-kicker">WORKSPACE SETTINGS</span><h2>Workspace preferences</h2><p>These preferences apply to your current session and reset when you refresh the app.</p></div><div className="settings-grid"><section className="panel"><div className="panel-head"><div><span className="section-kicker">APPEARANCE</span><h3>Interface</h3></div></div><div className="setting-row"><div><strong>HeartTrack dark theme</strong><small>Uses the dark color palette shown throughout HeartTrack.</small></div><span className="fixed-pill">On</span></div><div className="setting-row"><div><strong>Compact layout</strong><small>Reduce spacing on data-heavy screens.</small></div><button className={`toggle ${compact?'on':''}`} onClick={()=>setCompact(v=>!v)}><span/></button></div><div className="setting-row"><div><strong>Reduced motion</strong><small>Limit animations and moving visual effects.</small></div><button className={`toggle ${reduced?'on':''}`} onClick={()=>setReduced(v=>!v)}><span/></button></div></section><section className="panel"><div className="panel-head"><div><span className="section-kicker">WORKFLOW</span><h3>Guidance</h3></div></div><div className="setting-row"><div><strong>Field descriptions</strong><small>Show short explanations below assessment fields.</small></div><button className={`toggle ${tooltips?'on':''}`} onClick={()=>setTooltips(v=>!v)}><span/></button></div><div className="setting-row"><div><strong>Auto-save assessments</strong><small>Off. Assessment values are cleared when you refresh.</small></div><span className="fixed-pill fixed-pill--off">Off</span></div><div className="setting-row"><div><strong>Assessment history</strong><small>Off. Results remain available only for the current session.</small></div><span className="fixed-pill fixed-pill--off">Off</span></div></section><section className="panel panel--wide settings-brand-panel"><div className="panel-head"><div><span className="section-kicker">COLOR PALETTE</span><h3>HeartTrack colors</h3></div></div><div className="palette-row">{[['Obsidian','#05070B'],['Graphite','#0B1118'],['Deep Navy','#071E2C'],['Signal Cyan','#22E6FF'],['Pulse Blue','#19BCEB'],['Coral Alert','#FF6B66']].map(([n,c])=><div key={n}><span style={{background:c}}/><strong>{n}</strong><small>{c}</small></div>)}</div></section></div><div className="safety-banner"><Icon name="settings"/><div><strong>Settings reset automatically</strong><p>These preferences apply only to the current session and return to their defaults after a refresh.</p></div></div></div>
}
